diff --git a/resources/fps.rml b/resources/fps.rml
--- a/resources/fps.rml
+++ b/resources/fps.rml
@@ -1 +1 @@
-<rml><head><title>FPS</title><style>body { font-family: "Liberation Sans"; font-size: 16dp; color: #ffffff; background-color: #0b101cdd; position: absolute; top: 8dp; right: 8dp; width: 82dp; height: 24dp; padding: 5dp; pointer-events: none; z-index: 100; }</style></head><body><span id="count">FPS</span></body></rml>
+<rml><head><title>FPS</title><style>body { font-family: "Liberation Sans"; font-size: 14dp; color: #ffffff; background-color: #0b101cdd; position: absolute; top: 8dp; right: 8dp; min-width: 82dp; width: auto; height: auto; padding: 4dp 8dp; border-radius: 4dp; pointer-events: none; z-index: 100; white-space: nowrap; }</style></head><body><span id="count">FPS</span></body></rml>
diff --git a/resources/port-menu.rml b/resources/port-menu.rml
--- a/resources/port-menu.rml
+++ b/resources/port-menu.rml
@@ -1,8 +1,8 @@
 <rml><head><title>PC enhancements</title><link type="text/rcss" href="port-menu.rcss" /></head>
 <body>
-<div id="toolbar"><span class="wordmark">MELEE <span class="muted">/ PC</span></span><span class="toolbar-tab">SETTINGS</span><span class="toolbar-note">F1 TO CLOSE</span></div>
+<div id="toolbar"><span class="wordmark">MELEE <span class="muted">/ NX</span></span><span class="toolbar-tab">SETTINGS</span><span class="toolbar-note">- + R3 TO CLOSE</span></div>
 <div id="window">
-<div id="heading"><h1>PC settings</h1><span class="version"><span id="menu-version">v0.1.6-beta</span> · NTSC-U 1.02</span></div>
+<div id="heading"><h1>Melee NX settings</h1><span class="version"><span id="menu-version">v0.1.0-nx</span> · NTSC-U 1.02</span></div>
 <div id="tabstrip"><button id="tab-graphics" class="tab">Graphics</button><button id="tab-audio" class="tab">Audio &amp; interface</button><button id="tab-cheats" class="tab">Cheats</button><button id="tab-controls" class="tab">Controls</button></div>
 <div id="content">
 <div id="page-graphics" class="page">
@@ -27,7 +27,8 @@
 <div class="setting"><div class="description"><h3>Music volume</h3><p>Background music volume.</p></div><div class="slider-control"><input type="range" id="music-volume" min="0" max="100" step="5" value="100" /><span id="music-volume-val" class="slider-value">100%</span></div></div>
 <div class="setting"><div class="description"><h3>SFX volume</h3><p>Sound effects and combat audio volume.</p></div><div class="slider-control"><input type="range" id="sfx-volume" min="0" max="100" step="5" value="100" /><span id="sfx-volume-val" class="slider-value">100%</span></div></div>
 <div class="setting"><div class="description"><h3>Mute audio</h3></div><button id="mute">Mute</button></div>
-<div class="setting"><div class="description"><h3>Performance display</h3></div><button id="fps">FPS counter</button></div>
+<div class="setting"><div class="description"><h3>Performance display</h3><p>Off, FPS counter, or full realtime profiler overlay.</p></div><button id="fps">FPS counter</button></div>
+<div class="setting"><div class="description"><h3>Live performance</h3><p id="profiler-live-stats">FPS: -- | Sim: -- | Submit: -- | Wait: --</p></div></div>
 <div class="setting"><div class="description"><h3>UI scale</h3><p>Scale launcher and overlay elements.</p></div><div class="slider-control"><input type="range" id="scale" min="75" max="150" step="25" value="100" /><span id="scale-val" class="slider-value">100%</span></div></div>
 <div class="setting"><div class="description"><h3>Check for updates</h3><p id="port-update-status">Up to date</p></div><button id="port-check-update">Check now</button></div>
 </div>
@@ -61,6 +62,6 @@
 </div>
 </div>
 <div id="bottom"><p id="menu-status">Saved automatically.</p><button id="resume">Return to game</button></div>
-<div id="hints">ARROWS / D-PAD Navigate <span>ENTER / A Select</span><span>ESC / B / F1 Close</span><span>BACK Opens this menu</span></div>
+<div id="hints">ARROWS / D-PAD Navigate <span>A Select</span><span>B / - + R3 Close</span><span>- + R3 Opens this menu</span></div>
 </div>
 </body></rml>
diff --git a/src/pc/audio.c b/src/pc/audio.c
--- a/src/pc/audio.c
+++ b/src/pc/audio.c
@@ -461,8 +461,10 @@
     }
 
     /* Fast path for ratio == 0x10000 with non-zero phase: phase remains constant throughout. */
+    const float inv_scale = (1.0f / 32768.0f) * (1.0f / 32767.0f);
+    const float inv_phase = 1.0f / 65536.0f;
     if (ratio == 0x10000) {
-        float t = (float)v->frac * (1.0f / 65536.0f);
+        float t = (float)v->frac * inv_phase;
         for (int i = 0; i < AX_FRAME; i++) {
             s16 s;
             if (!next_sample(v, &s)) {
@@ -473,9 +475,8 @@
             }
             v->prev = v->cur;
             v->cur = s;
-            float smp = ((float)v->prev + t * (float)(v->cur - v->prev)) * (1.0f / 32768.0f);
-            float g = (float)vol * (1.0f / 32767.0f);
-            float sv = smp * g;
+            float smp = (float)v->prev + t * (float)(v->cur - v->prev);
+            float sv = smp * (float)vol * inv_scale;
             out[i * 2] += sv * vl;
             out[i * 2 + 1] += sv * vr;
             if (send_a) {
@@ -506,33 +507,16 @@
             if (!next_sample(v, &s)) {
                 pb->state = 0;
                 pb->ve.currentVolume = (u16)(vol < 0 ? 0 : vol > 32767 ? 32767 : vol);
-                /* The mirror the game reads back has to follow on this path
-                 * too: stopRange() matches a voice against the bank being
-                 * unloaded by pb.addr.currentAddress, and the HPS block swap
-                 * derives the stream position from it. Without this the
-                 * field stayed at the previous frame's value, reporting the
-                 * voice up to one frame short of where it really stopped. */
                 set_addr(&pb->addr.currentAddressHi, &pb->addr.currentAddressLo, v->cur_addr);
-                /* frac is deliberately left alone. It holds one whole
-                 * unsatisfied sample step plus the sub-sample phase, and
-                 * nothing at end-of-voice should discard that: on hardware
-                 * the DSP's SRC phase survives an end too, and the reset
-                 * arrives only with an explicit SRC block (AXSetVoiceSrc,
-                 * which the HPS block swap does supply and which sets frac
-                 * from currentAddressFrac). A voice restarted with only
-                 * AXSetVoiceCurrentAddr therefore keeps its phase, and the
-                 * retained whole step is what consumes the first sample of
-                 * the new block instead of skipping it. */
                 return;
             }
             v->prev = v->cur;
             v->cur = s;
             v->frac -= 0x10000;
         }
-        float t = (float)v->frac * (1.0f / 65536.0f);
-        float s = ((float)v->prev + t * (float)(v->cur - v->prev)) * (1.0f / 32768.0f);
-        float g = (float)vol * (1.0f / 32767.0f);
-        float sv = s * g;
+        float t = (float)v->frac * inv_phase;
+        float smp = (float)v->prev + t * (float)(v->cur - v->prev);
+        float sv = smp * (float)vol * inv_scale;
         out[i * 2] += sv * vl;
         out[i * 2 + 1] += sv * vr;
         if (send_a) {
@@ -757,6 +741,23 @@
             fprintf(stderr, "audio: SDL_OpenAudioDeviceStream failed: %s\n", SDL_GetError());
             return;
         }
+#if defined(__SWITCH__)
+        SDL_AudioSpec device_spec;
+        int device_frames = 0;
+        if (SDL_GetAudioDeviceFormat(SDL_GetAudioStreamDevice(s_stream), &device_spec, &device_frames)) {
+            pc_log_line("AUDIO driver %s device_hz %d format 0x%x channels %d frames %d mixer_hz %d neon %d",
+                        SDL_GetCurrentAudioDriver(), device_spec.freq, (unsigned)device_spec.format,
+                        device_spec.channels, device_frames, AX_RATE,
+#if defined(PC_AUDIO_SIMD_NEON)
+                        1
+#else
+                        0
+#endif
+            );
+        } else {
+            pc_log_line("AUDIO device format query failed: %s", SDL_GetError());
+        }
+#endif
         SDL_ResumeAudioStreamDevice(s_stream);
     }
 }
diff --git a/src/pc/file_cache.cpp b/src/pc/file_cache.cpp
--- a/src/pc/file_cache.cpp
+++ b/src/pc/file_cache.cpp
@@ -25,6 +25,8 @@
 #include <dolphin/dvd.h>
 #include <dolphin/os.h>
 
+#include "switch_io_lock.h"
+
 namespace {
 
 struct CachedEntry {
@@ -97,7 +99,12 @@
         size_t mb = static_cast<size_t>(strtoul(env_mb, nullptr, 10));
         if (mb <= 24)
             return PROFILE_LOW_RAM;
-        if (mb <= 96)
+        // Handheld (Switch) keeps its gentle Tier-1 prewarm + 5ms pacing even with
+        // a large byte budget. The budget (get_default_budget_bytes) is a separate
+        // knob from the profile; only budgets sized for a real desktop (> 320 MB)
+        // opt into the DESKTOP profile's aggressive 200us full-directory prewarm,
+        // which would saturate the Switch SD reader. See set_file_cache_budget().
+        if (mb <= 320)
             return PROFILE_HANDHELD;
         return PROFILE_DESKTOP;
     }
@@ -188,6 +195,7 @@
     // 1. Check if loose file exists
     std::string loose_path = resolve_loose_path(key.c_str());
     if (!loose_path.empty()) {
+        SWITCH_IO_LOCK();
         struct stat st;
         if (stat(loose_path.c_str(), &st) == 0 && S_ISREG(st.st_mode)) {
             std::ifstream in(loose_path, std::ios::binary);
@@ -312,6 +320,7 @@
     // Check loose file directory override
     std::string loose_path = resolve_loose_path(key.c_str());
     if (!loose_path.empty()) {
+        SWITCH_IO_LOCK();
         struct stat st;
         if (stat(loose_path.c_str(), &st) == 0 && S_ISREG(st.st_mode)) {
             std::ifstream in(loose_path, std::ios::binary);
@@ -353,6 +362,7 @@
 
     std::string loose_path = resolve_loose_path(key.c_str());
     if (!loose_path.empty()) {
+        SWITCH_IO_LOCK();
         struct stat st;
         if (stat(loose_path.c_str(), &st) == 0 && S_ISREG(st.st_mode)) {
             *size = static_cast<size_t>(st.st_size);
diff --git a/src/pc/launcher.cpp b/src/pc/launcher.cpp
--- a/src/pc/launcher.cpp
+++ b/src/pc/launcher.cpp
@@ -877,7 +877,15 @@
 }
 
 static std::filesystem::path pc_resources_path() {
-#if defined(__ANDROID__)
+#if defined(__SWITCH__)
+    if (access("romfs:/port-menu.rml", F_OK) == 0)
+        return "romfs:";
+    if (access("romfs:/resources/port-menu.rml", F_OK) == 0)
+        return "romfs:/resources";
+    if (access("sdmc:/switch/melee-nx/resources/port-menu.rml", F_OK) == 0)
+        return "sdmc:/switch/melee-nx/resources";
+    return "resources";
+#elif defined(__ANDROID__)
     return "";
 #else
     const char* base = SDL_GetBasePath();
@@ -892,7 +900,8 @@
             auto info = launcher::inspect_disc(command_line_disc);
             if (info.supported && aurora_dvd_open(command_line_disc)) {
                 pc_load_disc_fonts(command_line_disc);
-                prefs.disc = (std::string(command_line_disc).rfind("content://", 0) == 0) ?
+                prefs.disc = (std::string(command_line_disc).rfind("content://", 0) == 0 ||
+                              std::string(command_line_disc).find(":/") != std::string::npos) ?
                                  command_line_disc :
                                  std::filesystem::absolute(command_line_disc).string();
                 if (!launcher::save_preferences(config_path, prefs, error))
@@ -948,7 +957,8 @@
     // Set while the menu writes its own widgets: suppresses the Change and
     // Focus events that echo back, which would otherwise loop or chatter.
     bool quiet = false;
-    uint64_t last_fps = 0, last_axis_y = 0, last_axis_x = 0;
+    uint64_t last_fps = 0, last_axis_y = 0, last_axis_x = 0, last_prof_menu = 0;
+    bool back_held = false, r3_held = false;
     int tab = 0;
     u32 pad_port = 0;
     // -1 when idle, otherwise the mapping slot waiting for input. Axis slots
@@ -1105,7 +1115,12 @@
         slider("sfx-volume", prefs.sfx_volume * 100.0f);
         label("sfx-volume-val", std::to_string(int(prefs.sfx_volume * 100 + 0.5f)) + "%");
         label("mute", prefs.mute ? "On" : "Off");
-        label("fps", prefs.fps ? "On" : "Off");
+        if (prefs.fps == 0)
+            label("fps", "Off");
+        else if (prefs.fps == 1)
+            label("fps", "FPS only");
+        else
+            label("fps", "Full profiler");
         slider("scale", prefs.scale * 100.0f);
         label("scale-val", std::to_string(int(prefs.scale * 100 + 0.5f)) + "%");
         label("menu-version", pc::get_app_version());
@@ -1299,7 +1314,7 @@
         } else if (id == "mute")
             prefs.mute = !prefs.mute;
         else if (id == "fps")
-            prefs.fps = !prefs.fps;
+            prefs.fps = (prefs.fps + 1) % 3;
         else if (id == "scale") {
             prefs.scale = prefs.scale >= 1.5f ? 0.75f : prefs.scale + 0.25f;
             aurora::rmlui::set_ui_scale(prefs.scale);
@@ -1375,9 +1390,18 @@
             return;
         if (e.type == SDL_EVENT_GAMEPAD_BUTTON_DOWN) {
             const auto button = e.gbutton.button;
-            if (button == SDL_GAMEPAD_BUTTON_BACK && binding < 0) {
-                toggle();
-                return;
+            if (button == SDL_GAMEPAD_BUTTON_BACK) {
+                back_held = true;
+                if (r3_held && binding < 0) {
+                    toggle();
+                    return;
+                }
+            } else if (button == SDL_GAMEPAD_BUTTON_RIGHT_STICK) {
+                r3_held = true;
+                if (back_held && binding < 0) {
+                    toggle();
+                    return;
+                }
             }
             if (!open || binding >= 0)
                 return;  // a bind prompt owns every button
@@ -1410,6 +1434,12 @@
             default:
                 break;
             }
+        } else if (e.type == SDL_EVENT_GAMEPAD_BUTTON_UP) {
+            const auto button = e.gbutton.button;
+            if (button == SDL_GAMEPAD_BUTTON_BACK)
+                back_held = false;
+            else if (button == SDL_GAMEPAD_BUTTON_RIGHT_STICK)
+                r3_held = false;
         } else if (e.type == SDL_EVENT_GAMEPAD_AXIS_MOTION && open && binding < 0) {
             const auto value = e.gaxis.value;
             const auto now = SDL_GetTicks();
@@ -1515,13 +1545,38 @@
         port_menu.gamepad(*event);
 }
 extern "C" void pc_menu_update(void) {
+    PcProfilerStats stats{};
+    pc_profiler_get_stats(&stats);
+
+    if (port_menu.open && port_menu.document) {
+        if (SDL_GetTicks() - port_menu.last_prof_menu >= 250) {
+            auto* live = port_menu.document->GetElementById("profiler-live-stats");
+            if (live) {
+                char buf[160];
+                std::snprintf(buf, sizeof(buf),
+                    "FPS: %.1f (%.1fms) | Sim: %.1fms | Submit: %.1fms | Wait: %.1fms | Peak: %.1fms",
+                    stats.fps, stats.frame_ms, stats.sim_ms, stats.submit_ms, stats.wait_ms, stats.peak_ms);
+                live->SetInnerRML(buf);
+            }
+            port_menu.last_prof_menu = SDL_GetTicks();
+        }
+    }
+
     if (port_menu.counter) {
-        if (prefs.fps) {
+        if (prefs.fps > 0) {
             if (!port_menu.counter->IsVisible())
                 port_menu.counter->Show(Rml::ModalFlag::None, Rml::FocusFlag::None);
-            if (SDL_GetTicks() - port_menu.last_fps >= 500) {
-                port_menu.counter->GetElementById("count")->SetInnerRML(
-                    std::to_string(int(aurora_get_fps() + 0.5f)) + " FPS");
+            if (SDL_GetTicks() - port_menu.last_fps >= 250) {
+                if (prefs.fps == 1) {
+                    port_menu.counter->GetElementById("count")->SetInnerRML(
+                        std::to_string(int(stats.fps + 0.5f)) + " FPS");
+                } else {
+                    char buf[160];
+                    std::snprintf(buf, sizeof(buf),
+                        "%.1f FPS (%.1fms) | Sim: %.1fms | Submit: %.1fms | Wait: %.1fms",
+                        stats.fps, stats.frame_ms, stats.sim_ms, stats.submit_ms, stats.wait_ms);
+                    port_menu.counter->GetElementById("count")->SetInnerRML(buf);
+                }
                 port_menu.last_fps = SDL_GetTicks();
             }
         } else if (port_menu.counter->IsVisible())
diff --git a/src/pc/launcher.h b/src/pc/launcher.h
--- a/src/pc/launcher.h
+++ b/src/pc/launcher.h
@@ -12,6 +12,17 @@
 void pc_menu_toggle(void);
 void pc_menu_event(const union SDL_Event* event);
 bool pc_menu_is_open(void);
+
+typedef struct PcProfilerStats {
+    float fps;
+    float frame_ms;
+    float sim_ms;
+    float submit_ms;
+    float wait_ms;
+    float peak_ms;
+} PcProfilerStats;
+
+void pc_profiler_get_stats(PcProfilerStats* out_stats);
 #ifdef __cplusplus
 }
 #endif
diff --git a/src/pc/launcher_data.cpp b/src/pc/launcher_data.cpp
--- a/src/pc/launcher_data.cpp
+++ b/src/pc/launcher_data.cpp
@@ -26,6 +26,7 @@
 #endif
 #else
 #include <unistd.h>
+#include <fcntl.h>
 #endif
 
 #if defined(__has_include)
@@ -374,6 +375,13 @@
 
 bool save_preferences(
     const std::filesystem::path& path, const Preferences& prefs, std::string& error) {
+#if defined(__SWITCH__)
+    int fd = open(path.string().c_str(), O_WRONLY | O_CREAT | O_TRUNC, 0644);
+    if (fd < 0) {
+        error = "Could not save launcher settings: " + std::string(std::strerror(errno));
+        return false;
+    }
+#else
     // A sibling temporary file keeps replacement atomic on the preference filesystem.
     std::string pattern = path.string() + ".XXXXXX";
     int fd = mkstemp(pattern.data());
@@ -381,6 +389,7 @@
         error = "Could not save launcher settings: " + std::string(std::strerror(errno));
         return false;
     }
+#endif
     std::ostringstream text;
     text << "disc " << std::quoted(prefs.disc) << "\nvsync " << prefs.vsync << "\nfullscreen "
          << prefs.fullscreen << "\nscale " << prefs.scale << '\n'
@@ -409,6 +418,7 @@
         ok = false;
     if (close(fd) != 0)
         ok = false;
+#if !defined(__SWITCH__)
     std::error_code ec;
     if (ok) {
         std::filesystem::rename(pattern, path, ec);
@@ -418,6 +428,11 @@
         std::filesystem::remove(pattern, ec);
         error = "Could not save launcher settings.";
     }
+#else
+    if (!ok) {
+        error = "Could not save launcher settings.";
+    }
+#endif
     return ok;
 }
 }  // namespace launcher
diff --git a/src/melee/lb/lb_01F8.c b/src/melee/lb/lb_01F8.c
--- a/src/melee/lb/lb_01F8.c
+++ b/src/melee/lb/lb_01F8.c
@@ -61,18 +61,28 @@
     GXInitTexObj(&lbl_804335B8.tex0, lbl_804335B8.x20, lbl_804335B8.x6C,
                  lbl_804335B8.x6E, 1, 0, 0, 0);
     GXInitTexObjLOD(&lbl_804335B8.tex0, 0, 0, 0.0F, 0.0F, 0.0F, 0, 0, 0);
+#ifdef TARGET_PC
+    /* Re-decoded into the same buffer every frame: never cache (see lbmthp.c). */
+    GXInitTexObjNoCache(&lbl_804335B8.tex0, GX_TRUE);
+#endif
     GXLoadTexObj(&lbl_804335B8.tex0, 0);
 
     /* Second texture - half size */
     GXInitTexObj(&lbl_804335B8.tex1, lbl_804335B8.x44, (u16) (*pWidth >> 1),
                  (u16) (*pHeight >> 1), 1, 0, 0, 0);
     GXInitTexObjLOD(&lbl_804335B8.tex1, 0, 0, 0.0F, 0.0F, 0.0F, 0, 0, 0);
+#ifdef TARGET_PC
+    GXInitTexObjNoCache(&lbl_804335B8.tex1, GX_TRUE);
+#endif
     GXLoadTexObj(&lbl_804335B8.tex1, 1);
 
     /* Third texture - half size */
     GXInitTexObj(&lbl_804335B8.tex2, lbl_804335B8.x68, (u16) (*pWidth >> 1),
                  (u16) (*pHeight >> 1), 1, 0, 0, 0);
     GXInitTexObjLOD(&lbl_804335B8.tex2, 0, 0, 0.0F, 0.0F, 0.0F, 0, 0, 0);
+#ifdef TARGET_PC
+    GXInitTexObjNoCache(&lbl_804335B8.tex2, GX_TRUE);
+#endif
     GXLoadTexObj(&lbl_804335B8.tex2, 2);
 
     HSD_SObjLib_803A49E0(gobj, arg1);
diff --git a/src/melee/lb/lbmthp.c b/src/melee/lb/lbmthp.c
--- a/src/melee/lb/lbmthp.c
+++ b/src/melee/lb/lbmthp.c
@@ -647,6 +647,12 @@
                      GX_CLAMP, GX_CLAMP, 0U);
         GXInitTexObjLOD(&streamPlayer->unk_178, GX_NEAR, GX_NEAR, 0.0f, 0.0f,
                         0.0f, 0U, 0U, GX_ANISO_1);
+#ifdef TARGET_PC
+        /* Re-decoded into the same buffer every frame: never cache it, or the
+         * resolve-side object cache serves a stale/mismatched luma-chroma plane
+         * and the video renders corrupted. */
+        GXInitTexObjNoCache(&streamPlayer->unk_178, GX_TRUE);
+#endif
         GXLoadTexObj(&streamPlayer->unk_178, GX_TEXMAP0);
 
         GXInitTexObj(&streamPlayer->unk_198, streamPlayer->unk_54,
@@ -654,6 +660,9 @@
                      GX_TF_I8, GX_CLAMP, GX_CLAMP, 0U);
         GXInitTexObjLOD(&streamPlayer->unk_198, GX_NEAR, GX_NEAR, 0.0f, 0.0f,
                         0.0f, 0U, 0U, GX_ANISO_1);
+#ifdef TARGET_PC
+        GXInitTexObjNoCache(&streamPlayer->unk_198, GX_TRUE);
+#endif
         GXLoadTexObj(&streamPlayer->unk_198, GX_TEXMAP1);
 
         GXInitTexObj(&streamPlayer->unk_1B8, streamPlayer->unk_58,
@@ -661,6 +670,9 @@
                      GX_TF_I8, GX_CLAMP, GX_CLAMP, 0U);
         GXInitTexObjLOD(&streamPlayer->unk_1B8, GX_NEAR, GX_NEAR, 0.0f, 0.0f,
                         0.0f, 0U, 0U, GX_ANISO_1);
+#ifdef TARGET_PC
+        GXInitTexObjNoCache(&streamPlayer->unk_1B8, GX_TRUE);
+#endif
         GXLoadTexObj(&streamPlayer->unk_1B8, GX_TEXMAP2);
 
         HSD_SObjLib_803A49E0(gobj, arg1);
