diff --git a/src/melee/db/db.h b/src/melee/db/db.h
--- a/src/melee/db/db.h
+++ b/src/melee/db/db.h
@@ -79,7 +79,7 @@
 /* 4D6B24 */ extern DiscU32* db_submotion_names; ///< char*[] on disc
 /* 4D6B28 */ extern DiscU32* db_motionstate_names; ///< char*[] on disc
 /* 4D6B2C */ extern DiscU32* db_bonus_names;       ///< char*[] on disc
-#define DB_NAME(tbl, i) ((char*) (uintptr_t) (tbl)[i].v)
+#define DB_NAME(tbl, i) (DP(char, (tbl)[i].v))
 /* 4D6B30 */ extern u16 db_gameLaunchButtonState;
 /* 4D6B80 */ extern int db_MiscVisualEffectsStatus;
 
diff --git a/src/melee/ft/fighter.c b/src/melee/ft/fighter.c
--- a/src/melee/ft/fighter.c
+++ b/src/melee/ft/fighter.c
@@ -1304,7 +1304,7 @@
                 ftAnim_8006E9B4(gobj);
                 {
                     FighterPartsTable* parts =
-                        (FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v;
+                        DP(FighterPartsTable, ftPartsTable[fp->kind].v);
                     if ((bone_index != 0) &&
                         (u32) bone_index < parts->parts_num &&
                         (*unk_byte_ptr)[0] != 0U)
diff --git a/src/melee/ft/ftCo_800C7CA0.c b/src/melee/ft/ftCo_800C7CA0.c
--- a/src/melee/ft/ftCo_800C7CA0.c
+++ b/src/melee/ft/ftCo_800C7CA0.c
@@ -161,7 +161,7 @@
 {
     size_t i;
     int idx;
-    FighterPartsTable* parts = (FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v;
+    FighterPartsTable* parts = DP(FighterPartsTable, ftPartsTable[fp->kind].v);
     for (i = idx = 0; i < parts->parts_num; idx++, i++) {
         FighterBone* bone = &fp->parts[idx];
         if (bone->flags_b1) {
diff --git a/src/melee/ft/ftanim.c b/src/melee/ft/ftanim.c
--- a/src/melee/ft/ftanim.c
+++ b/src/melee/ft/ftanim.c
@@ -523,7 +523,7 @@
     Fighter* fp = GET_FIGHTER(gobj);
     int i;
 
-    FighterPartsTable* parts = (FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v;
+    FighterPartsTable* parts = DP(FighterPartsTable, ftPartsTable[fp->kind].v);
     if (fp->x8A4_animBlendFrames == 0.0F) {
         for (i = 0; i < parts->parts_num; i++) {
             if (fp->parts[i].flags_b1 && !fp->parts[i].flags_b0 &&
@@ -562,7 +562,7 @@
 
     if (fp->x8A4_animBlendFrames == 0.0F) {
         int i;
-        for (i = 0; i < ((FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v)->parts_num; i++) {
+        for (i = 0; i < (DP(FighterPartsTable, ftPartsTable[fp->kind].v))->parts_num; i++) {
             if (fp->parts[i].flags_b1 && !fp->parts[i].flags_b0 &&
                 !fp->parts[i].flags_b5)
             {
@@ -937,7 +937,7 @@
 void ftAnim_8006FE9C(Fighter* fp, Fighter_Part start, float t, float t_inv)
 {
     int i;
-    for (i = start; i < ((FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v)->parts_num; i++) {
+    for (i = start; i < (DP(FighterPartsTable, ftPartsTable[fp->kind].v))->parts_num; i++) {
         if (fp->parts[i].flags_b1 && !fp->parts[i].flags_b0 &&
             !fp->parts[i].flags_b5)
         {
@@ -954,7 +954,7 @@
 void ftAnim_8006FF74(Fighter* fp, Fighter_Part start)
 {
     int i;
-    for (i = start; i < ((FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v)->parts_num; i++) {
+    for (i = start; i < (DP(FighterPartsTable, ftPartsTable[fp->kind].v))->parts_num; i++) {
         if (fp->parts[i].flags_b1 && !fp->parts[i].flags_b0 &&
             !fp->parts[i].flags_b5)
         {
@@ -1145,7 +1145,7 @@
     for (i = 0; i < (signed) ARRAY_SIZE(fp->x8B0); i++) {
         temp_r4 = &fp->x8B0[i];
         if (temp_r4->x11 != -1) {
-            struct ftData_x1C* temp_r26 = (struct ftData_x1C*) (uintptr_t) DP(DiscU32, fp->ft_data->x1C)[i].v;
+            struct ftData_x1C* temp_r26 = DP(struct ftData_x1C, DP(DiscU32, fp->ft_data->x1C)[i].v);
             u8* var_r29 = DP(u8, temp_r26->x4);
 
             temp_r4->x8 += temp_r4->xC;
@@ -1275,16 +1275,16 @@
     struct ftData_x1C* temp_r29;
 
     temp_r30 = &fp->x8B0[arg1];
-    temp_r29 = (struct ftData_x1C*) (uintptr_t) DP(DiscU32, fp->ft_data->x1C)[arg1].v;
+    temp_r29 = DP(struct ftData_x1C, DP(DiscU32, fp->ft_data->x1C)[arg1].v);
     temp_r30->x11 = arg2;
     temp_r30->x4 = arg3;
     temp_r30->x8 = 0.0F;
     if (arg3) {
-        temp_r30->xC = lb_8000BFF0((HSD_AnimJoint*) (uintptr_t) DP(DiscU32, temp_r29->x8)[arg2].v) / arg3;
+        temp_r30->xC = lb_8000BFF0(DP(HSD_AnimJoint, DP(DiscU32, temp_r29->x8)[arg2].v)) / arg3;
     } else {
         temp_r30->xC = 0.0F;
     }
-    ftAnim_80070904(fp, temp_r29->x0, (HSD_AnimJoint*) (uintptr_t) DP(DiscU32, temp_r29->x8)[arg2].v);
+    ftAnim_80070904(fp, temp_r29->x0, DP(HSD_AnimJoint, DP(DiscU32, temp_r29->x8)[arg2].v));
 }
 
 void ftAnim_80070C48(Fighter_GObj* gobj, s32 arg1)
@@ -1325,8 +1325,8 @@
     if (r30->x11 == -1) {
         return;
     }
-    r28 = (struct ftData_x1C*) (uintptr_t) DP(DiscU32, fp->ft_data->x1C)[arg1].v;
-    some_inline(fp, r28->x0, (HSD_AnimJoint*) (uintptr_t) DP(DiscU32, r28->x8)[r30->x11].v);
+    r28 = DP(struct ftData_x1C, DP(DiscU32, fp->ft_data->x1C)[arg1].v);
+    some_inline(fp, r28->x0, DP(HSD_AnimJoint, DP(DiscU32, r28->x8)[r30->x11].v));
 
     r30->x11 = -1;
     if (fp->x590 != NULL) {
@@ -1359,7 +1359,7 @@
     for (i = 0; i < ARRAY_SIZE(fp->x8B0); i++) {
         struct Fighter_x8B0_t* slot = &fp->x8B0[i];
         if (slot->x11 != -1) {
-            struct ftData_x1C* data = (struct ftData_x1C*) (uintptr_t) DP(DiscU32, fp->ft_data->x1C)[i].v;
+            struct ftData_x1C* data = DP(struct ftData_x1C, DP(DiscU32, fp->ft_data->x1C)[i].v);
             u8* parts_list = DP(u8, data->x4);
             int j;
 
diff --git a/src/melee/ft/ftcmdscript.c b/src/melee/ft/ftcmdscript.c
--- a/src/melee/ft/ftcmdscript.c
+++ b/src/melee/ft/ftcmdscript.c
@@ -473,7 +473,7 @@
  */
 void ftCo_800B4880(Fighter* fp, int script_idx)
 {
-    u8* cmd = (u8*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->cmdscripts)[script_idx].v;
+    u8* cmd = DP(u8, DP(DiscU32, Fighter_804D64FC->cmdscripts)[script_idx].v);
     while (*cmd != CpuCmd_Done) {
         ftCo_800B463C(fp, *cmd);
         if (*cmd > CpuCmd_OneArgEnd) {
diff --git a/src/melee/ft/ftcpuattack.c b/src/melee/ft/ftcpuattack.c
--- a/src/melee/ft/ftcpuattack.c
+++ b/src/melee/ft/ftcpuattack.c
@@ -1817,7 +1817,7 @@
     if (fp->ground_or_air == GA_Air) {
         if (ftCo_800B89CC(fp)) {
             result =
-                ftCo_800B4AB0(fp, target, ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x8)[fp->kind].v));
+                ftCo_800B4AB0(fp, target, (DP(void, DP(DiscU32, Fighter_804D64FC->x8)[fp->kind].v)));
             if (result != 0) {
                 cpu->xA4 = result;
                 return true;
@@ -1839,7 +1839,7 @@
                 tmp->xC8++;
             }
         }
-        result = ftCo_800B4AB0(fp, target, ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x4)[fp->kind].v));
+        result = ftCo_800B4AB0(fp, target, (DP(void, DP(DiscU32, Fighter_804D64FC->x4)[fp->kind].v)));
         if (result != 0) {
             cpu->xA4 = result;
             return true;
@@ -1877,7 +1877,7 @@
                 break;
             }
         }
-        result = ftCo_800B52AC(fp, target, ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x18)[fp->kind].v),
+        result = ftCo_800B52AC(fp, target, (DP(void, DP(DiscU32, Fighter_804D64FC->x18)[fp->kind].v)),
                                weapon_reach);
         if (result != 0) {
             cpu->xA4 = result;
@@ -1885,7 +1885,7 @@
         }
     }
     if (cpu->level > 5 && ftCo_800B9F6C(target)) {
-        result = ftCo_800B4AB0(fp, target, ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x10)[fp->kind].v));
+        result = ftCo_800B4AB0(fp, target, (DP(void, DP(DiscU32, Fighter_804D64FC->x10)[fp->kind].v)));
         if (result != 0) {
             cpu->xA4 = result;
             return true;
@@ -1910,21 +1910,21 @@
         }
     }
     if (var_r0 != 0) {
-        result = ftCo_800B4AB0(fp, target, ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x1C)[fp->kind].v));
+        result = ftCo_800B4AB0(fp, target, (DP(void, DP(DiscU32, Fighter_804D64FC->x1C)[fp->kind].v)));
         if (result != 0) {
             cpu->xA4 = result;
             cpu->xF8_b7 = 1;
             return true;
         }
     }
-    result = ftCo_800B4AB0(fp, target, ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x4)[fp->kind].v));
+    result = ftCo_800B4AB0(fp, target, (DP(void, DP(DiscU32, Fighter_804D64FC->x4)[fp->kind].v)));
     if (result != 0) {
         cpu->xA4 = result;
         return true;
     }
     if (cpu->x50 != NULL) {
         result = ftCo_800B5AB0(fp, cpu->x50,
-                               ((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->x14)[fp->kind].v));
+                               (DP(void, DP(DiscU32, Fighter_804D64FC->x14)[fp->kind].v)));
         if (result != 0) {
             cpu->xA4 = result;
             return true;
@@ -2342,7 +2342,7 @@
         return false;
     }
     if (temp_f31 < 0.08726646192371845 && temp_f31 > -0.08726646192371845) {
-        temp_r31->xA4 = ftCo_800B6208(((void*) (uintptr_t) DP(DiscU32, Fighter_804D64FC->xC)[fp->kind].v));
+        temp_r31->xA4 = ftCo_800B6208((DP(void, DP(DiscU32, Fighter_804D64FC->xC)[fp->kind].v)));
         if (temp_r31->xA4 != 0) {
             return true;
         }
diff --git a/src/melee/ft/ftdata.c b/src/melee/ft/ftdata.c
--- a/src/melee/ft/ftdata.c
+++ b/src/melee/ft/ftdata.c
@@ -1764,7 +1764,7 @@
                         lbArq_80014BD0(temp_r4_2, fp->x59C,
                                        OSRoundUp32B(temp_r3->x8), 0, 0);
                     } else {
-                        memcpy(fp->x59C, (void*) (uintptr_t) temp_r4_2, temp_r3->x8);
+                        memcpy(fp->x59C, DP(void, temp_r4_2), temp_r3->x8);
                     }
                     temp_ret_2 =
                         HSD_ArchiveParse(&sp14, fp->x59C->x0, temp_r3->x8);
@@ -1777,7 +1777,7 @@
             } else {
                 fp->x590 = NULL;
             }
-            fp->x5A4 = (void*) (uintptr_t) temp_r3->x14;
+            fp->x5A4 = DP(void, temp_r3->x14);
         }
     }
 }
@@ -1817,7 +1817,7 @@
                         lbArq_80014BD0(temp_r4_2, arg0->x5A0,
                                        OSRoundUp32B(temp_r3->x8), 0, 0);
                     } else {
-                        memcpy(arg0->x5A0, (void*) (uintptr_t) temp_r4_2, temp_r3->x8);
+                        memcpy(arg0->x5A0, DP(void, temp_r4_2), temp_r3->x8);
                     }
                     temp_ret_2 =
                         HSD_ArchiveParse(&sp10, arg0->x5A0->x0, temp_r3->x8);
@@ -1830,7 +1830,7 @@
             } else {
                 arg0->x598 = 0;
             }
-            arg0->x5A8 = (void*) (uintptr_t) temp_r3->x14;
+            arg0->x5A8 = DP(void, temp_r3->x14);
         }
         return arg0->x598;
     }
diff --git a/src/melee/ft/ftlib.c b/src/melee/ft/ftlib.c
--- a/src/melee/ft/ftlib.c
+++ b/src/melee/ft/ftlib.c
@@ -393,8 +393,7 @@
                      "out=(%g,%g,%g) cur_pos=(%g,%g,%g) box=%p dst=%p\n",
                      (int) fp->player_id, (int) fp->kind, (int) fp->motion_id,
                      fp->x34_scale.y, (int) i,
-                     ((struct FighterPartsTable*) (uintptr_t)
-                          ftPartsTable[fp->kind].v)->parts_num,
+                     (DP(struct FighterPartsTable, ftPartsTable[fp->kind].v))->parts_num,
                      (void*) bone, bone_world.x,
                      bone_world.y, bone_world.z, offset.x, offset.y, offset.z,
                      v->x, v->y, v->z, fp->cur_pos.x, fp->cur_pos.y,
diff --git a/src/melee/ft/ftmetal.c b/src/melee/ft/ftmetal.c
--- a/src/melee/ft/ftmetal.c
+++ b/src/melee/ft/ftmetal.c
@@ -21,7 +21,7 @@
 
     PAD_STACK(8);
 
-    for (i = 0; i < ((FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v)->parts_num; i++) {
+    for (i = 0; i < (DP(FighterPartsTable, ftPartsTable[fp->kind].v))->parts_num; i++) {
         if (fp->parts[i].flags_b1) {
             FighterBone* bone = &fp->parts[i];
             jobj = bone->joint;
@@ -81,7 +81,7 @@
     int i;
     HSD_JObj* jobj;
 
-    for (i = 0; i < ((FighterPartsTable*) (uintptr_t) ftPartsTable[fp->kind].v)->parts_num; i++) {
+    for (i = 0; i < (DP(FighterPartsTable, ftPartsTable[fp->kind].v))->parts_num; i++) {
         if (fp->parts[i].flags_b1) {
             jobj = fp->parts[i].joint;
             dobj = HSD_JObjGetDObj(jobj);
diff --git a/src/melee/ft/ftparts.c b/src/melee/ft/ftparts.c
--- a/src/melee/ft/ftparts.c
+++ b/src/melee/ft/ftparts.c
@@ -18,7 +18,7 @@
 #include <sysdolphin/baselib/gobj.h>
 #include <sysdolphin/baselib/jobj.h>
 static inline FighterPartsTable* get_parts_tbl(int kind) {
-    return (FighterPartsTable*) (uintptr_t) ftPartsTable[kind].v;
+    return DP(FighterPartsTable, ftPartsTable[kind].v);
 }
 #include <sysdolphin/baselib/mtx.h>
 #include <sysdolphin/baselib/perf.h>
@@ -521,10 +521,10 @@
         HSD_ASSERTREPORT(627, 0, "fighter parts model num over!\n");
     }
 
-    vis->xC[0] = (FtPartsVisLookup*) (uintptr_t) (vis_table[costume_id][0].v ? DP(void, vis_table[costume_id][0].v) : DP(void, vis_table[0][0].v));
-    vis->xC[1] = (FtPartsVisLookup*) (uintptr_t) (vis_table[costume_id][1].v ? DP(void, vis_table[costume_id][1].v) : DP(void, vis_table[0][1].v));
-    vis->xC[2] = (FtPartsVisLookup*) (uintptr_t) (vis_table[costume_id][2].v ? DP(void, vis_table[costume_id][2].v) : DP(void, vis_table[0][2].v));
-    vis->xC[3] = (FtPartsVisLookup*) (uintptr_t) (vis_table[costume_id][3].v ? DP(void, vis_table[costume_id][3].v) : DP(void, vis_table[0][3].v));
+    vis->xC[0] = DP(FtPartsVisLookup, (vis_table[costume_id][0].v) ? DP(void, vis_table[costume_id][0].v) : DP(void, vis_table[0][0].v));
+    vis->xC[1] = DP(FtPartsVisLookup, (vis_table[costume_id][1].v) ? DP(void, vis_table[costume_id][1].v) : DP(void, vis_table[0][1].v));
+    vis->xC[2] = DP(FtPartsVisLookup, (vis_table[costume_id][2].v) ? DP(void, vis_table[costume_id][2].v) : DP(void, vis_table[0][2].v));
+    vis->xC[3] = DP(FtPartsVisLookup, (vis_table[costume_id][3].v) ? DP(void, vis_table[costume_id][3].v) : DP(void, vis_table[0][3].v));
     vis->xC[4] = 0;
     vis->cleared[0] = true;
     vis->cleared[1] = true;
@@ -756,7 +756,7 @@
     int i;
     struct Fighter_804D6540_t* temp_r3;
 
-    temp_r3 = (struct Fighter_804D6540_t*) (uintptr_t) Fighter_804D6540[ftkind].v;
+    temp_r3 = DP(struct Fighter_804D6540_t, Fighter_804D6540[ftkind].v);
     if (temp_r3 != NULL && temp_r3->x4 != 0) {
         var_r3 = DP(struct Fighter_804D6540_x0_t, temp_r3->x0);
         for (i = 0; i < temp_r3->x4; i++, var_r3++) {
diff --git a/src/melee/gm/gm_1879.c b/src/melee/gm/gm_1879.c
--- a/src/melee/gm/gm_1879.c
+++ b/src/melee/gm/gm_1879.c
@@ -94,7 +94,7 @@
         }
         frame = data->x37.frame_counter * 0x12C;
         HSD_CObjRemoveAnim(cobj);
-        HSD_CObjAddAnim(cobj, (HSD_CameraAnim*) (uintptr_t) DP(DiscU32, data->x4->anims)[0].v);
+        HSD_CObjAddAnim(cobj, DP(HSD_CameraAnim, DP(DiscU32, data->x4->anims)[0].v));
         HSD_CObjReqAnim(cobj, (f32) frame);
     }
     HSD_CObjAnim(cobj);
@@ -314,7 +314,7 @@
     HSD_GObjObject_80390A70(gobj, HSD_GObj_CameraKind, cobj);
     GObj_SetupGXLinkMax(gobj, (GObj_RenderFunc) (Event) Camera_800304E0, 8);
     HSD_GObj_SetupProc(gobj, fn_80187910, 0);
-    HSD_CObjAddAnim(cobj, (HSD_CameraAnim*) (uintptr_t) DP(DiscU32, data->x4->anims)[0].v);
+    HSD_CObjAddAnim(cobj, DP(HSD_CameraAnim, DP(DiscU32, data->x4->anims)[0].v));
     HSD_CObjReqAnim(cobj, 0.0f);
 
     {
diff --git a/src/melee/gm/gmevent.c b/src/melee/gm/gmevent.c
--- a/src/melee/gm/gmevent.c
+++ b/src/melee/gm/gmevent.c
@@ -135,7 +135,7 @@
 DISC_ASSERT_SIZE(struct gm_804D6900_t, 0x28);
 
 /* gm_804D6900[0] is the in-archive `gm_804D6900_t*[]` level table. */
-#define LV(tbl, i) ((struct gm_804D6900_t*) (uintptr_t) (tbl)[i].v)
+#define LV(tbl, i) (DP(struct gm_804D6900_t, (tbl)[i].v))
 #define EV_X4(l) DP(struct gm_804D6900_x4_t, (l)->x4)
 #define EV_INIT(l) DP(struct gm_evinit, (l)->evinit)
 #define EV_BONUS(l) DP(struct gm_evbonus, (l)->evbonus)
diff --git a/src/melee/gm/gmstaffroll.c b/src/melee/gm/gmstaffroll.c
--- a/src/melee/gm/gmstaffroll.c
+++ b/src/melee/gm/gmstaffroll.c
@@ -1230,9 +1230,7 @@
         GObj_SetupGXLinkMax(gobj, fn_801AA854, 5);
         gobj->gxlink_prios = 0x189;
         HSD_CObjAddAnim(
-            cobj, (HSD_CameraAnim*) (uintptr_t) DP(
-                      DiscU32, DP(struct SceneCameraDesc, gm_804D6840->cameras)->anims)[0]
-                      .v);
+            cobj, DP(HSD_CameraAnim, DP( DiscU32, DP(struct SceneCameraDesc, gm_804D6840->cameras)->anims)[0] .v));
         HSD_CObjReqAnim(cobj, 0.0F);
         HSD_GObj_SetupProc(gobj, fn_801AA7F8, 0);
     }
@@ -1266,9 +1264,7 @@
         GObj_SetupGXLink(gobj, HSD_GObj_FogCallback, 3, 0);
         HSD_Fog_8037DE7C(
             fog, DP(HSD_AObjDesc,
-                    ((HSD_CameraAnim*) (uintptr_t) DP(
-                         DiscU32, DP(struct SceneFogDesc, gm_804D6840->fogs)->anims)[0]
-                         .v)
+                    (DP(HSD_CameraAnim, DP( DiscU32, DP(struct SceneFogDesc, gm_804D6840->fogs)->anims)[0] .v))
                         ->aobjdesc));
         HSD_FogReqAnim(fog, 0.0F);
         HSD_GObj_SetupProc(gobj, fn_801AC67C, 0);
@@ -1276,7 +1272,7 @@
     {
         HSD_GObj* gobj = GObj_Create(3, 5, 0);
         DynamicModelDesc* model =
-            (DynamicModelDesc*) (uintptr_t) DP(DiscU32, gm_804D6840->models)[1].v;
+            DP(DynamicModelDesc, DP(DiscU32, gm_804D6840->models)[1].v);
         HSD_JObj* jobj = HSD_JObjLoadJoint(DP(HSD_Joint, model->joint));
         gm_804D682C = jobj;
         HSD_GObjObject_80390A70(gobj, HSD_GObj_JObjKind, jobj);
@@ -1317,7 +1313,7 @@
     {
         final_gobj = GObj_Create(14, 15, 0);
         DynamicModelDesc* model =
-            (DynamicModelDesc*) (uintptr_t) DP(DiscU32, gm_804D6840->models)[0].v;
+            DP(DynamicModelDesc, DP(DiscU32, gm_804D6840->models)[0].v);
         jobj = HSD_JObjLoadJoint(DP(HSD_Joint, model->joint));
         HSD_GObjObject_80390A70(final_gobj, HSD_GObj_JObjKind, jobj);
         GObj_SetupGXLink(final_gobj, HSD_GObj_JObjCallback, gx_link, 0);
diff --git a/src/melee/if/ifnametag.c b/src/melee/if/ifnametag.c
--- a/src/melee/if/ifnametag.c
+++ b/src/melee/if/ifnametag.c
@@ -194,15 +194,15 @@
     DP_SET(un_804A1ED0.joint, DP(HSD_Joint, d->joint));
     if (d->anims) {
         DP_SET(un_804A1ED0.animjoint,
-               (HSD_AnimJoint*) (uintptr_t) DP(DiscU32, d->anims)[0].v);
+               DP(HSD_AnimJoint, DP(DiscU32, d->anims)[0].v));
     }
     if (d->matanims) {
         DP_SET(un_804A1ED0.matanim_joint,
-               (HSD_MatAnimJoint*) (uintptr_t) DP(DiscU32, d->matanims)[0].v);
+               DP(HSD_MatAnimJoint, DP(DiscU32, d->matanims)[0].v));
     }
     if (d->shapeanims) {
         DP_SET(un_804A1ED0.shapeanim_joint,
-               (HSD_ShapeAnimJoint*) (uintptr_t) DP(DiscU32, d->shapeanims)[0].v);
+               DP(HSD_ShapeAnimJoint, DP(DiscU32, d->shapeanims)[0].v));
     }
 }
 #ifdef MUST_MATCH
diff --git a/src/melee/if/ifstatus.c b/src/melee/if/ifstatus.c
--- a/src/melee/if/ifstatus.c
+++ b/src/melee/if/ifstatus.c
@@ -240,7 +240,7 @@
 
     ones_jobj = state->jobjs[Ones];
     digit = state->damage_percent % 10;
-    HSD_AnimJoint* aj = (HSD_AnimJoint*) (uintptr_t) ((DiscU32*) anim_base[0])[0].v;
+    HSD_AnimJoint* aj = DP(HSD_AnimJoint, ((DiscU32*) anim_base[0])[0].v);
     HSD_TexAnim* digit_anim = get_digit_texanim(aj);
     HSD_TObjAddAnimAll(ones_jobj->u.dobj->mobj->tobj, digit_anim);
     HSD_TObjReqAnimAll(ones_jobj->u.dobj->mobj->tobj, 2.0F * digit);
@@ -422,7 +422,7 @@
         digit_jobj = state->jobjs[Percent];
         if (digit_jobj != NULL) {
             tobj = digit_jobj->u.dobj->mobj->tobj;
-            HSD_AnimJoint* aj = (HSD_AnimJoint*) (uintptr_t) ((DiscU32*) anim_base[0])[0].v;
+            HSD_AnimJoint* aj = DP(HSD_AnimJoint, ((DiscU32*) anim_base[0])[0].v);
             HSD_TObjAddAnimAll(tobj, get_percent_texanim(aj));
             if (Player_GetMoreFlagsBit2((s8) state->player_slot)) {
                 HSD_TObjReqAnimAll(tobj, 1.0F);
@@ -445,7 +445,7 @@
 
         post_digit_jobj = state->jobjs[Ones];
         digit = state->damage_percent % 10;
-        HSD_AnimJoint* aj = (HSD_AnimJoint*) (uintptr_t) ((DiscU32*) anim_joints[0])[0].v;
+        HSD_AnimJoint* aj = DP(HSD_AnimJoint, ((DiscU32*) anim_joints[0])[0].v);
         HSD_TexAnim* digit_anim = get_digit_texanim(aj);
         HSD_TObjAddAnimAll(post_digit_jobj->u.dobj->mobj->tobj, digit_anim);
         HSD_TObjReqAnimAll(post_digit_jobj->u.dobj->mobj->tobj, 2.0F * digit);
@@ -706,7 +706,7 @@
     }
     if (state->jobjs[3] != NULL) {
         tobj = state->jobjs[3]->u.dobj->mobj->tobj;
-        HSD_AnimJoint* aj = (HSD_AnimJoint*) (uintptr_t) ((DiscU32*) anim_base[0])[0].v;
+        HSD_AnimJoint* aj = DP(HSD_AnimJoint, ((DiscU32*) anim_base[0])[0].v);
         HSD_TObjAddAnimAll(tobj, get_percent_texanim(aj));
         if (Player_GetMoreFlagsBit2((s8) state->player_slot) != 0) {
             HSD_TObjReqAnimAll(tobj, 1.0f);
@@ -870,8 +870,8 @@
     arch = ifAll_GetArchive();
     lbArchive_LoadSections(*arch, (void**) &num_tbl, num_models_name,
                            (void**) &mrk_tbl, mrk_models_name, 0);
-    num = (DynamicModelDesc*) (uintptr_t) num_tbl[0].v;
-    mrk = (DynamicModelDesc*) (uintptr_t) mrk_tbl[0].v;
+    num = DP(DynamicModelDesc, num_tbl[0].v);
+    mrk = DP(DynamicModelDesc, mrk_tbl[0].v);
     hud->unk258 = DP(HSD_Joint, num->joint);
     hud->jobj_desc_parent = DP(DiscU32, num->anims);
     hud->janim_selection_joints = (HSD_AnimJoint*) DP(DiscU32, num->matanims);
diff --git a/src/melee/if/ifstock.c b/src/melee/if/ifstock.c
--- a/src/melee/if/ifstock.c
+++ b/src/melee/if/ifstock.c
@@ -1025,7 +1025,7 @@
     lbArchive_LoadSections(*ifAll_GetArchive(), (void**) &scene_models,
                            ifStock_SceneModels, 0);
     stock->x0 = scene_models;
-    stock->x4 = (DynamicModelDesc*) (uintptr_t) scene_models[1].v;
+    stock->x4 = DP(DynamicModelDesc, scene_models[1].v);
     ifStock_804A1ACC.x108 = NULL;
     ifStock_804A1ACC.x0 = 0;
     gobj = GObj_Create(14, 15, 0);
diff --git a/src/melee/if/iftime.c b/src/melee/if/iftime.c
--- a/src/melee/if/iftime.c
+++ b/src/melee/if/iftime.c
@@ -26,7 +26,7 @@
     DiscU32* countdown_timer_models; /* DynamicModelDesc*[] in the archive */
 } ifTime_data;
 static DynamicModelDesc ifTime_match_timer_models;
-#define IFTIME_COUNTDOWN_MODEL(x) ((DynamicModelDesc*) (uintptr_t) (x)->countdown_timer_models[0].v)
+#define IFTIME_COUNTDOWN_MODEL(x) (DP(DynamicModelDesc, (x)->countdown_timer_models[0].v))
 
 static bool ifTime_LoadModels(void)
 {
@@ -35,7 +35,7 @@
     lbArchive_LoadSections(*ifAll_GetArchive(), (void*) &ScInfTim_scene_models,
                            "ScInfTim_scene_models",
                            &ifTime_data.countdown_timer_models, "tdsce", 0);
-    model = (DynamicModelDesc*) (uintptr_t) ScInfTim_scene_models[0].v;
+    model = DP(DynamicModelDesc, ScInfTim_scene_models[0].v);
     if (model != NULL) {
         ifTime_match_timer_models.joint = model->joint;
         ifTime_match_timer_models.anims = model->anims;
diff --git a/src/melee/if/soundtest.c b/src/melee/if/soundtest.c
--- a/src/melee/if/soundtest.c
+++ b/src/melee/if/soundtest.c
@@ -701,7 +701,7 @@
 
 /// .sbss
 /* 4D6DA8 */ static DiscS32* un_804D6DA8; ///< [0..4],[7]: ints; [5],[6]: int* to int[]
-#define SOUNDTEST_SYM_ARR(i) ((DiscS32*) (uintptr_t) (u32) un_804D6DA8[i].v)
+#define SOUNDTEST_SYM_ARR(i) (DP(DiscS32, (u32) un_804D6DA8[i].v))
 /* 4D6DAC */ static int un_804D6DAC;
 /* 4D6DB0 */ static int un_804D6DB0;
 /* 4D6DB4 */ static int un_804D6DB4;
diff --git a/src/melee/if/types.h b/src/melee/if/types.h
--- a/src/melee/if/types.h
+++ b/src/melee/if/types.h
@@ -274,7 +274,7 @@
     HSD_JObj* jobj_d;
     struct ifStock_804A1378_x204 x204[6];
 };
-#define IFSTOCK_MODEL0(stock) ((DynamicModelDesc*) (uintptr_t) (stock)->x0[0].v)
+#define IFSTOCK_MODEL0(stock) (DP(DynamicModelDesc, (stock)->x0[0].v))
 
 struct ifStock_804A1774 {
     char x0;
diff --git a/src/melee/lb/lb_00B0.c b/src/melee/lb/lb_00B0.c
--- a/src/melee/lb/lb_00B0.c
+++ b/src/melee/lb/lb_00B0.c
@@ -369,17 +369,17 @@
     HSD_ShapeAnimJoint* phi_r6;
 
     if (arg3 != NULL) {
-        phi_r4 = (HSD_AnimJoint*) (uintptr_t) arg3[i].v;
+        phi_r4 = DP(HSD_AnimJoint, arg3[i].v);
     } else {
         phi_r4 = NULL;
     }
     if (arg4 != NULL) {
-        phi_r5 = (HSD_MatAnimJoint*) (uintptr_t) arg4[i].v;
+        phi_r5 = DP(HSD_MatAnimJoint, arg4[i].v);
     } else {
         phi_r5 = NULL;
     }
     if (arg5 != NULL) {
-        phi_r6 = (HSD_ShapeAnimJoint*) (uintptr_t) arg5[i].v;
+        phi_r6 = DP(HSD_ShapeAnimJoint, arg5[i].v);
     } else {
         phi_r6 = NULL;
     }
diff --git a/src/melee/lb/lbcardgame.c b/src/melee/lb/lbcardgame.c
--- a/src/melee/lb/lbcardgame.c
+++ b/src/melee/lb/lbcardgame.c
@@ -61,7 +61,7 @@
 #define _p(x) (state.x)
 
 #ifdef TARGET_PC
-#define GET_ICON_DATA(idx) ((void*) (uintptr_t) _p(icon_data)[idx].v)
+#define GET_ICON_DATA(idx) (DP(void, _p(icon_data)[idx].v))
 #else
 #define GET_ICON_DATA(idx) (_p(icon_data)[idx])
 #endif
@@ -318,8 +318,7 @@
 
         gobj = GObj_Create(HSD_GOBJ_CLASS_MEMCARD, HSD_GOBJ_PLINK_MEMCARD, 0);
 #ifdef TARGET_PC
-        DynamicModelDesc* model0 = (DynamicModelDesc*) (uintptr_t)
-            DP(DiscU32, _p(scene_data)->models)[0].v;
+        DynamicModelDesc* model0 = DP(DynamicModelDesc, DP(DiscU32, _p(scene_data)->models)[0].v);
         jobj = HSD_JObjLoadJoint(DP(HSD_Joint, model0->joint));
 #else
         jobj = HSD_JObjLoadJoint(_p(scene_data)->models[0]->joint);
diff --git a/src/melee/pl/player.c b/src/melee/pl/player.c
--- a/src/melee/pl/player.c
+++ b/src/melee/pl/player.c
@@ -2019,7 +2019,7 @@
 
     lbArchive_LoadSymbols(str_PdPmdat_start_of_data, (void**) &sp8,
                           str_plLoadCommonData, NULL);
-    pl_804D6470 = (pl_804D6470_t*) (uintptr_t) sp8->v;
+    pl_804D6470 = DP(pl_804D6470_t, sp8->v);
 }
 
 void Player_80036E20(CharacterKind ckind, HSD_Archive* archive, s32 arg2)
diff --git a/src/melee/sc/types.h b/src/melee/sc/types.h
--- a/src/melee/sc/types.h
+++ b/src/melee/sc/types.h
@@ -8,7 +8,7 @@
  * symbols, EfXxData). Runtime code that fills one from lbArchive_LoadSections
  * must load into void* locals and DP_SET the slots.
  * Pointer arrays: `DISC_PTR(DiscU32) anims` is `HSD_AnimJoint*[]` on disc,
- * read as `(HSD_AnimJoint*) (uintptr_t) DP(DiscU32, d->anims)[i].v`. */
+ * read as `DP(HSD_AnimJoint, DP(DiscU32, d->anims)[i].v)`. */
 
 /// Model with a single animation or no animation
 struct DISC_STRUCT StaticModelDesc {
diff --git a/src/pc/disc.h b/src/pc/disc.h
--- a/src/pc/disc.h
+++ b/src/pc/disc.h
@@ -49,9 +49,42 @@
 void* pc_resolve_ext_ptr(uint32_t id);
 void pc_disc_ptr_overflow(const void* p, const char* file, int line) __attribute__((noreturn));
 
+/* MEM1's 4GB window.
+ *
+ * Hosts that can place MEM1 below 4GB leave these zero, the window is off, and
+ * a slot is a plain host address exactly as before. Horizon cannot: libnx maps
+ * its heap far above 4GB (0x1f_63xx_xxxx on hardware) and there is no way to
+ * ask for anything lower -- svcMapMemory only accepts a destination inside the
+ * kernel's stack region, which a 39-bit address space places below 4GB
+ * essentially never. So on Switch a slot instead means "offset inside whatever
+ * 4GB window MEM1 landed in", and pc_resolve_dp puts the high half back.
+ *
+ * This is what keeps the raw 32-bit relocation arithmetic correct without
+ * touching any of it -- HSD_ArchiveParse's Locate, axdriver's three table
+ * fixups, particle.c's three -- because inside a single window only the low 32
+ * bits ever differ. AllocMEM1 guarantees MEM1 does not straddle a boundary. */
+extern uintptr_t pc_mem1_hi; /* MEM1 base, low 32 bits cleared */
+extern uint32_t pc_mem1_lo;  /* MEM1 base, low 32 bits only */
+extern uint32_t pc_mem1_len; /* MEM1 size; 0 disables the window */
+
+void pc_disc_set_mem1_window(const void* base, uint32_t size);
+
+/* Whether p is a host address a 32-bit slot can hold. With the window off this
+ * is the original "fits in 32 bits" test. */
+static inline int pc_dp_in_window(const void* p) {
+    if (pc_mem1_len == 0) {
+        return !((uintptr_t)p >> 32);
+    }
+    return ((uintptr_t)p & ~(uintptr_t)0xFFFFFFFFu) == pc_mem1_hi &&
+           (uint32_t)(uintptr_t)p - pc_mem1_lo < pc_mem1_len;
+}
+
 static inline uint32_t pc_encode_dp(const void* p) {
     if (!p)
         return 0;
+    if (pc_dp_in_window(p)) {
+        return (uint32_t)(uintptr_t)p;
+    }
     if (!((uintptr_t)p >> 32)) {
         return (uint32_t)(uintptr_t)p;
     }
@@ -61,6 +94,12 @@
 static inline void* pc_resolve_dp(uint32_t slot) {
     if ((slot & 0xFF000000u) == 0x02000000u) {
         return pc_resolve_ext_ptr(slot & 0x00FFFFFFu);
+    }
+    /* Only slots inside MEM1's window get the high half back. Anything else is
+     * an ARAM offset -- the game tells those apart by being below 16MB, see
+     * PC_IS_ARAM_ADDR -- and has to stay the small integer it already is. */
+    if (pc_mem1_len != 0 && (uint32_t)(slot - pc_mem1_lo) < pc_mem1_len) {
+        return (void*)(pc_mem1_hi | (uintptr_t)slot);
     }
     return (void*)(uintptr_t)slot;
 }
diff --git a/src/sysdolphin/baselib/archive.c b/src/sysdolphin/baselib/archive.c
--- a/src/sysdolphin/baselib/archive.c
+++ b/src/sysdolphin/baselib/archive.c
@@ -10,11 +10,13 @@
     DiscU32* ptr;
 
     /* Pointer slots stay big-endian in the archive image and hold absolute
-     * host addresses. Every slot is 32 bits wide, so an image loaded above
-     * 4GB would silently truncate every pointer in it; this is the same
-     * check DP_SET makes, applied to the one site that bypasses DP_SET. */
+     * host addresses, truncated to the 32 bits a slot has room for. The image
+     * therefore has to sit where pc_resolve_dp can reconstruct it: below 4GB
+     * on hosts that can place MEM1 there, inside MEM1's 4GB window on Switch.
+     * Same test DP_SET makes, applied to one of the several sites that bypass
+     * DP_SET and relocate slots by raw 32-bit addition instead. */
 #ifdef TARGET_PC
-    if (archive->header.nb_reloc != 0 && ((uintptr_t) archive->data >> 32)) {
+    if (archive->header.nb_reloc != 0 && !pc_dp_in_window(archive->data)) {
         pc_disc_ptr_overflow(archive->data, __FILE__, __LINE__);
     }
 #endif
diff --git a/src/sysdolphin/baselib/particle.c b/src/sysdolphin/baselib/particle.c
--- a/src/sysdolphin/baselib/particle.c
+++ b/src/sysdolphin/baselib/particle.c
@@ -205,7 +205,7 @@
 
     /* Phase 2: Fix cmdList kind bits */
     for (i = num; i < num2; i++) {
-        HSD_PSCmdList* cmd = (HSD_PSCmdList*) (uintptr_t) base[i].v;
+        HSD_PSCmdList* cmd = DP(HSD_PSCmdList, base[i].v);
         if (cmd != NULL) {
             cmd->kind = (cmd->kind & 0xF1FFFFFF) | 0x08000000;
         }
@@ -222,7 +222,7 @@
         }
 
         for (k = 0; k < num_groups; k++) {
-            HSD_PSTexGroup* tg = (HSD_PSTexGroup*) (uintptr_t) groups[k].v;
+            HSD_PSTexGroup* tg = DP(HSD_PSTexGroup, groups[k].v);
             u32 fmt;
             u32 end;
             if (tg == NULL) {
@@ -257,7 +257,7 @@
             HSD_PSFormGroup* fg;
             s32 fi;
             relocate_slot(&groups[i], formBank);
-            fg = (HSD_PSFormGroup*) (uintptr_t) groups[i].v;
+            fg = DP(HSD_PSFormGroup, groups[i].v);
             if (fg == NULL) {
                 continue;
             }
diff --git a/src/sysdolphin/baselib/psstructs.h b/src/sysdolphin/baselib/psstructs.h
--- a/src/sysdolphin/baselib/psstructs.h
+++ b/src/sysdolphin/baselib/psstructs.h
@@ -92,9 +92,9 @@
 
 /* Per-bank lookup tables point into the banks: arrays of 32-bit slots holding
  * host addresses (or 0) after relocation. */
-#define PS_TEXGROUP(bank, idx) ((HSD_PSTexGroup*) (uintptr_t) psTexGroupArray[bank][idx].v)
-#define PS_FORMGROUP(bank, idx) ((HSD_PSFormGroup*) (uintptr_t) psNumCmdList[bank][idx].v)
-#define PS_CMDLIST(bank, idx) ((HSD_PSCmdList*) (uintptr_t) ptclref_804D0E5C[bank][idx].v)
+#define PS_TEXGROUP(bank, idx) (DP(HSD_PSTexGroup, psTexGroupArray[bank][idx].v))
+#define PS_FORMGROUP(bank, idx) (DP(HSD_PSFormGroup, psNumCmdList[bank][idx].v))
+#define PS_CMDLIST(bank, idx) (DP(HSD_PSCmdList, ptclref_804D0E5C[bank][idx].v))
 
 enum PS_AppStatus {
     PS_APPSTATUS_ONCE = 1,
