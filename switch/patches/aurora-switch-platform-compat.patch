diff --git a/include/dolphin/ppc_math.h b/include/dolphin/ppc_math.h
--- a/include/dolphin/ppc_math.h
+++ b/include/dolphin/ppc_math.h
@@ -126,11 +126,16 @@
 
 // One Newton-Raphson step
 static inline float ppc_rsqrte(float x) {
+#if defined(__SWITCH__) || defined(__aarch64__)
+    if (__builtin_expect(x <= 0.0f, 0)) return 0.0f;
+    return 1.0f / sqrtf(x);
+#else
     double rsqrt_d = frsqrte((double)x);
     float nwork0 = (float)(rsqrt_d * rsqrt_d);
     float nwork1 = (float)(rsqrt_d * 0.5);
     nwork0 = fmaf(-nwork0, x, 3.0f);
     return nwork0 * nwork1;
+#endif
 }
 
 #endif // DOLPHIN_PPC_MATH_H
diff --git a/lib/dolphin/mtx/mtx.c b/lib/dolphin/mtx/mtx.c
--- a/lib/dolphin/mtx/mtx.c
+++ b/lib/dolphin/mtx/mtx.c
@@ -2,6 +2,10 @@
 
 #include <assert.h>
 #include <math.h>
+
+#if defined(__aarch64__) || defined(__ARM_NEON)
+#include <arm_neon.h>
+#endif
 
 void C_MTXIdentity(Mtx m) {
   assert(m && "MtxIdentity():  NULL Mtx 'm' ");
@@ -23,6 +27,11 @@
   assert(src && "MTXCopy():  NULL MtxPtr 'src' ");
   assert(dst && "MTXCopy():  NULL MtxPtr 'dst' ");
   if (src != dst) {
+#if defined(__aarch64__) || defined(__ARM_NEON)
+    vst1q_f32(dst[0], vld1q_f32(src[0]));
+    vst1q_f32(dst[1], vld1q_f32(src[1]));
+    vst1q_f32(dst[2], vld1q_f32(src[2]));
+#else
     dst[0][0] = src[0][0];
     dst[0][1] = src[0][1];
     dst[0][2] = src[0][2];
@@ -35,10 +44,35 @@
     dst[2][1] = src[2][1];
     dst[2][2] = src[2][2];
     dst[2][3] = src[2][3];
+#endif
   }
 }
 
 void C_MTXConcat(const Mtx a, const Mtx b, Mtx ab) {
+#if defined(__aarch64__) || defined(__ARM_NEON)
+  const float32x4_t b0 = vld1q_f32(b[0]);
+  const float32x4_t b1 = vld1q_f32(b[1]);
+  const float32x4_t b2 = vld1q_f32(b[2]);
+
+  float32x4_t r0 = {0.0f, 0.0f, 0.0f, a[0][3]};
+  r0 = vfmaq_n_f32(r0, b0, a[0][0]);
+  r0 = vfmaq_n_f32(r0, b1, a[0][1]);
+  r0 = vfmaq_n_f32(r0, b2, a[0][2]);
+
+  float32x4_t r1 = {0.0f, 0.0f, 0.0f, a[1][3]};
+  r1 = vfmaq_n_f32(r1, b0, a[1][0]);
+  r1 = vfmaq_n_f32(r1, b1, a[1][1]);
+  r1 = vfmaq_n_f32(r1, b2, a[1][2]);
+
+  float32x4_t r2 = {0.0f, 0.0f, 0.0f, a[2][3]};
+  r2 = vfmaq_n_f32(r2, b0, a[2][0]);
+  r2 = vfmaq_n_f32(r2, b1, a[2][1]);
+  r2 = vfmaq_n_f32(r2, b2, a[2][2]);
+
+  vst1q_f32(ab[0], r0);
+  vst1q_f32(ab[1], r1);
+  vst1q_f32(ab[2], r2);
+#else
   Mtx mTmp;
   MtxPtr m;
 
@@ -70,6 +104,7 @@
   if (m == mTmp) {
     C_MTXCopy(mTmp, ab);
   }
+#endif
 }
 
 void C_MTXConcatArray(const Mtx a, const Mtx* srcBase, Mtx* dstBase, u32 count) {
diff --git a/lib/dolphin/mtx/mtxvec.c b/lib/dolphin/mtx/mtxvec.c
--- a/lib/dolphin/mtx/mtxvec.c
+++ b/lib/dolphin/mtx/mtxvec.c
@@ -2,6 +2,14 @@
 #include <dolphin/mtx.h>
 
 void C_MTXMultVec(const Mtx m, const Vec* src, Vec* dst) {
+#if defined(__aarch64__) || defined(__ARM_NEON)
+  const float sx = src->x;
+  const float sy = src->y;
+  const float sz = src->z;
+  dst->x = m[0][3] + (m[0][0] * sx + m[0][1] * sy + m[0][2] * sz);
+  dst->y = m[1][3] + (m[1][0] * sx + m[1][1] * sy + m[1][2] * sz);
+  dst->z = m[2][3] + (m[2][0] * sx + m[2][1] * sy + m[2][2] * sz);
+#else
   Vec vTmp;
 
   assert(m && "MTXMultVec():  NULL MtxPtr 'm' ");
@@ -14,24 +22,13 @@
   dst->x = vTmp.x;
   dst->y = vTmp.y;
   dst->z = vTmp.z;
+#endif
 }
 
 void C_MTXMultVecArray(const Mtx m, const Vec* srcBase, Vec* dstBase, u32 count) {
   u32 i;
-  Vec vTmp;
-
-  assert(m && "MTXMultVecArray():  NULL MtxPtr 'm' ");
-  assert(srcBase && "MTXMultVecArray():  NULL VecPtr 'srcBase' ");
-  assert(dstBase && "MTXMultVecArray():  NULL VecPtr 'dstBase' ");
-  assert(count > 1 && "MTXMultVecArray():  count must be greater than 1.");
-
-  for(i = 0; i < count; i++) {
-    vTmp.x = m[0][3] + ((m[0][2] * srcBase->z) + ((m[0][0] * srcBase->x) + (m[0][1] * srcBase->y)));
-    vTmp.y = m[1][3] + ((m[1][2] * srcBase->z) + ((m[1][0] * srcBase->x) + (m[1][1] * srcBase->y)));
-    vTmp.z = m[2][3] + ((m[2][2] * srcBase->z) + ((m[2][0] * srcBase->x) + (m[2][1] * srcBase->y)));
-    dstBase->x = vTmp.x;
-    dstBase->y = vTmp.y;
-    dstBase->z = vTmp.z;
+  for (i = 0; i < count; i++) {
+    C_MTXMultVec(m, srcBase, dstBase);
     srcBase++;
     dstBase++;
   }
diff --git a/lib/dolphin/thp/THPDec.cpp b/lib/dolphin/thp/THPDec.cpp
--- a/lib/dolphin/thp/THPDec.cpp
+++ b/lib/dolphin/thp/THPDec.cpp
@@ -300,14 +300,26 @@
 public:
   BitReader(const u8* data, size_t byteOffset) noexcept : mData{data}, mBitPosition{byteOffset * 8} {}
 
-  u32 read(u8 count) noexcept {
-    u32 value = 0;
-    for (u8 i = 0; i < count; ++i) {
-      const size_t bytePosition = mBitPosition >> 3;
-      value = (value << 1) | ((mData[bytePosition] >> (7 - (mBitPosition & 7))) & 1);
-      ++mBitPosition;
-    }
-    return value;
+  inline u32 read(u8 count) noexcept {
+    if (count == 0) return 0;
+    if (count == 1) {
+      const size_t pos = mBitPosition++;
+      return (mData[pos >> 3] >> (7 - (pos & 7))) & 1;
+    }
+    const size_t bytePos = mBitPosition >> 3;
+    const size_t bitOffset = mBitPosition & 7;
+    mBitPosition += count;
+    uint32_t b0 = mData[bytePos];
+    uint32_t b1 = mData[bytePos + 1];
+    uint32_t b2 = mData[bytePos + 2];
+    uint32_t b3 = mData[bytePos + 3];
+    uint32_t val = (b0 << 24) | (b1 << 16) | (b2 << 8) | b3;
+    return (val << bitOffset) >> (32 - count);
+  }
+
+  inline u32 read_bit() noexcept {
+    const size_t pos = mBitPosition++;
+    return (mData[pos >> 3] >> (7 - (pos & 7))) & 1;
   }
 
   void byte_align() noexcept { mBitPosition = (mBitPosition + 7) & ~size_t{7}; }
@@ -320,7 +332,7 @@
 bool decode_huffman(BitReader& reader, const HuffmanTable& table, u8& symbol) noexcept {
   u32 code = 0;
   for (size_t length = 1; length <= 16; ++length) {
-    code = (code << 1) | reader.read(1);
+    code = (code << 1) | reader.read_bit();
     const u32 firstCode = table.firstCodes[length];
     const u32 count = table.counts[length];
     if (code >= firstCode && code - firstCode < count) {
